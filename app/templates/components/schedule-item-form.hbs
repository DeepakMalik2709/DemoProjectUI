
                    <div class="box-body">
                   		
                        <!-- <div class="form-group {{if titleValidation 'has-error'}}">                               	                                          
                            {{input type="text" value=model.title class="form-control" placeholder="Enter schedule  title" }}
                            {{#if titleValidation}}<label class="control-label"> Schedule title is mandatory.</label>{{/if}}  
                        </div>  -->
                        <div class="form-group ">                               	                                          
                            {{input type="text" value=model.title class="form-control" placeholder="Enter schedule title" }}
                        </div> 
                       
                        <div class="form-group">                                               
                            {{input type="text" value=model.location class="form-control" placeholder="Enter schedule location" }}
                        </div>    
       
                        <div class="form-group">
                    		{{task-item-deadline item=model fieldName='fromDate' title="Click to add start date"}} 
                       </div> 
                         <div class="form-group">
                           {{task-item-deadline item=model fieldName='toDate' title="Click to add end date"}}   
     				   </div>  
     				       
            <div class="form-group">
        <label class="col-sm-2 control-label">All day event</label>
        <div class="col-sm-10">
          {{input type="checkbox" checked=model.allDayEvent class="" }}
        </div> 
    </div>
  
    <div class="clearfix"></div>
     				   
     <!-- 				    <div class="form-group">	
     				   		<button  class="btn btn-round btn-white"  {{action 'addAllDays' }}  >All</button>
     				     {{#each days as |day|}}
     				     	<button class="btn btn-round btn-white btn-{{if day.isActive 'success' 'default'}}" {{action 'addDay' day }}>{{day.name}}</button>
     				      {{/each}}
						</div>	 -->		
                         <div class="form-group mtop10" >
                          {{#power-select-multiple selected=selectedGroups  options=myGroups  
	                        placeholder="Please, select Group"
  							  onchange=(action (mut selectedGroups))  as |attendees| }}
							  {{attendees.name}}
							{{/power-select-multiple}}
							
					  </div>                   
                        {{new-file-upload files=model.files item=model}}
						<div class="form-group">
	                            <label for="exampleInputFile">Schedule desc</label> <small>Write something about schedule</small>                              
	                          {{textarea type="text" value=model.comment class="form-control" placeholder="Description"}}                              
	                    </div><!-- ./row -->
	                    
                        <div class="box-footer">
	                        <button  class="btn btn-primary" {{action 'saveEvent' model}}>{{buttonLabel}}</button>
	                     
	                        
	                      <!-- <button  class="btn btn-default" {{action 'cancelClicked' item}}>Cancel</button> -->
			              
			            
	                    </div>
                   </div><!-- /.box-body -->
                
                
              