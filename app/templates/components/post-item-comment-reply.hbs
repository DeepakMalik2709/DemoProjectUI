                 <li class="comment">
                                <a class="pull-left" href="javascript:void(0);">
                                    <img class="avatar" src="{{item.photoUrl}}" alt="avatar">
                                </a>
                                <div class="comment-body">
                                    <div class="comment-heading">
                                         <h4 class="user">{{item.createdByName}}</h4>
                                <h5 class="time">{{item.createdDisplayTime}}</h5>
                                    </div>
                                   <p class="{{unless item.isEditing 'show' 'hide'}}">{{{item.comment}}}</p>
                <p class="{{if item.isEditing 'show' 'hide'}}">
                {{post-comment-box  className="edit-comment"  save="updateCommentReply" esc-key='cancelEditing'  value=item.comment autoCompleteList=myList disabled=item.isSaving}}</p>
              
                                    <footer class="comment__footer"><menu class="comment-footer__menu">
									{{#if canEdit}}
								<li class="reply {{unless item.isEditing 'show' 'hide'}}" {{action "editCommentReply"}} data-role="reply-link"><a href="javascript:void(0);"
									data-action="reply"> <span class="text">Edit</span></a></li>
									<li class="reply {{if item.isEditing 'show' 'hide'}}" {{action "cancelEditing"}} data-role="reply-link"><a href="javascript:void(0);"
									data-action="reply"> <span class="text">Cancel Editing</span></a></li>
								<li class="bullet" aria-hidden="true">•</li>
								{{/if}}
								{{#if canDelete}}
									<li class="reply" data-role="reply-link" {{action "deleteCommentReply"}}><a href="javascript:void(0);"
									data-action="reply"> <span class="text">Delete</span></a></li>
								{{/if}}
								
							</menu>
									</footer>
                                </div>
                            </li> 